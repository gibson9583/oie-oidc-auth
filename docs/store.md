# OIDC Authentication

Adds generic OpenID Connect SSO to the web administrator while preserving named-user engine audit attribution. Supports discovery/JWKS validation, JIT provisioning, durable subject binding, local-login fallback, and optional RBAC role mapping. Requires matching OIDC configuration in the native web administrator.
